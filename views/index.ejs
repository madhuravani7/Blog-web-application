<%- include("partials/header.ejs") %>
<ul>
    <% posts.forEach(post => { %>
        <li>
            <h2><%= post.title %></h2>
            <p><%= post.content %></p>
        </li>
    <% }); %>
</ul>
